# Keep the current runnable site in ordinary Git so cloning the repo restores dist/ directly.
# Only intentionally archived files placed under archive/heavy/ use Git LFS.
archive/heavy/** filter=lfs diff=lfs merge=lfs -text
