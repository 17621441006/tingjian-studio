name: Verify current site and original media
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install audio verification dependency
        run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - run: npm ci
      - run: npm run build && npm run check && node scripts/prepare-hosting.mjs
      - run: node scripts/check-v28-floors.mjs && node scripts/check-v26-designs.mjs && node scripts/check-v25-objects.mjs && node scripts/check-v24-projection.mjs && node scripts/check-v23-photo-floor.mjs && node scripts/check-v22-floors.mjs && node scripts/check-v21-model.mjs && node scripts/check-v19-music.mjs && node scripts/check-assets.mjs && node scripts/check-v18-vr.mjs && node scripts/check-v17-ui.mjs && node scripts/check-v17-floors.mjs && node scripts/check-v16-assets.mjs && node scripts/check-v16-model.mjs
