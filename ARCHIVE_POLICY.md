# 历史方案归档分支

此分支用于保留“历史方案 / 第二标签”相关的旧功能、旧源码和需要时才恢复的大型素材。

## 归档原则

- `main` 只保留当前装修设计网站运行所需的源码与 `dist/` 资产。
- 历史方案中重复的大图、旧导出包、旧验证截图不重复进入 `main`。
- 单文件超过 100 MB 的历史归档如确有必要保存，使用 Git LFS；不把完整旧 ZIP 再嵌套提交到普通 Git 历史。
- 已可由现有图片、源码或在线站点恢复的重复素材，只保留 manifest / credits / provenance，不再重复占仓库体积。
- 目标：整个仓库尽量控制在 1 GB 以内。

## 当前说明

该分支创建于 v17 源码同步之后，作为后续历史素材归档锚点。当前主站需要的二进制资产将优先补到 `main/dist/`，历史重复包不进入主分支。
